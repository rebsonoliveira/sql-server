-- =============================================
-- With Named Set
-- =============================================

With 
		Set <set_name, mdx_name,> AS 
		'<set, mdx_expression,>'

Select	<row_axis, mdx_set,> on Columns,
		<set_name, mdx_name,> on Rows

From	<from_clause, mdx_name,>
Where	<where_clause, mdx_set,>	
